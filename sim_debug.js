// Wait! `tests/replay.spec.js` timed out on `await expect(controls).toBeVisible();`.
// Maybe `laps.length >= 2` is false in the playwright test!
// Let's add a console log in `renderTrackMap` or just run the test in headed mode?
